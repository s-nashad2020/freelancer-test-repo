{
	"name": "reai-email-worker",
	"version": "1.1.0",
	"description": "Cloudflare Email Worker that forwards voucher attachments to the REAI API",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy"
	},
	"type": "module",
	"engines": {
		"node": ">=18"
	},
	"dependencies": {
		"postal-mime": "^2.4.3"
	},
	"devDependencies": {
		"wrangler": "^4.22.0"
	}
}
